var Malta = require("malta");
var m = Malta.get().check(["#in/index.js", "out", "-vars=test/vars.json"]).start(
     function (o) {
        console.log('File content: ' + o.content);
     }
);
m.listenCallback('script_multi.js', () => {
    console.log('changed')
});
